codecov:
  branch: master

coverage:
  precision: 2
  round: down
  range: "60...100"

  status:
    project:
      default:
        target: auto

    patch:
      default:
        target: auto

    changes: no

comment: off
